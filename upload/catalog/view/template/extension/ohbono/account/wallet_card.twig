<div class="card ohbono-wallet-card mb-3">
    <div class="card-body">
        <div class="small text-muted">{{ text_balance }}</div>
        <div class="fs-4 fw-bold">{{ balance }}</div>

        <a href="{{ wallet_url }}" class="btn btn-primary mt-3">
            {{ button_view_wallet }}
        </a>
    </div>
</div>
