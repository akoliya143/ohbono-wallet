# Commit 0060 — Storefront Wallet Cleanup

## Added

- Simplified wallet dashboard
- Current balance focus
- Full history CTA
- Consistent customer-only access
- Reduced duplicate transaction rendering

## Git

```bash
git add .
git commit -m "feat(storefront): add secure wallet transaction detail"

git add .
git commit -m "feat(storefront): link wallet history to transaction details"

git add .
git commit -m "refactor(storefront): simplify wallet dashboard"

git push
```

## Next

Commit 0061 will focus on customer wallet notifications and transactional
messaging, including wallet credit/debit notices and order-related wallet
activity.
