# OHBONO Wallet — Batch 0067–0069

This batch introduces asynchronous email delivery.

## Database

The installer migration for this batch should create:

`wallet_email_queue`

Recommended fields:

- queue_id
- customer_id
- transaction_id
- notification_type
- subject
- message
- status
- attempts
- available_at
- date_added
- date_started
- date_sent
- last_error

## Required installer SQL

```sql
CREATE TABLE IF NOT EXISTS `oc_wallet_email_queue` (
  `queue_id` INT(11) NOT NULL AUTO_INCREMENT,
  `customer_id` INT(11) NOT NULL,
  `transaction_id` INT(11) NOT NULL,
  `notification_type` VARCHAR(64) NOT NULL,
  `subject` VARCHAR(255) NOT NULL,
  `message` TEXT NOT NULL,
  `status` VARCHAR(16) NOT NULL DEFAULT 'pending',
  `attempts` INT(11) NOT NULL DEFAULT 0,
  `available_at` DATETIME NOT NULL,
  `date_added` DATETIME NOT NULL,
  `date_started` DATETIME NULL DEFAULT NULL,
  `date_sent` DATETIME NULL DEFAULT NULL,
  `last_error` TEXT NULL,
  PRIMARY KEY (`queue_id`),
  UNIQUE KEY `uniq_wallet_email_transaction_type`
    (`transaction_id`, `notification_type`),
  KEY `idx_wallet_email_status_available`
    (`status`, `available_at`),
  KEY `idx_wallet_email_customer`
    (`customer_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

Replace `oc_` with the configured OpenCart `DB_PREFIX`.

## Next

0067–0069 establish the queue foundation. The next batch should add the actual
cron entry/controller and hardened queue locking before production deployment.
