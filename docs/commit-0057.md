# Commit 0057 — Customer Wallet Dashboard Refinement

## Added

- Simplified customer wallet dashboard
- Current balance
- Recent transactions
- Full history button
- Currency-aware formatting
- Responsive card/table presentation

## Git

```bash
git add .
git commit -m "feat(storefront): add full customer wallet history"

git add .
git commit -m "feat(storefront): add wallet history account navigation"

git add .
git commit -m "refactor(storefront): refine customer wallet dashboard"

git push
```

## Next

Commit 0058 will add storefront transaction detail access, secure transaction
lookup, order-context links and customer-friendly transaction descriptions.
