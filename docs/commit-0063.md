# Commit 0063 — Notification Reliability

Added idempotent notification creation, transaction linkage, indexes, and isolation of notification failures from financial mutations.

## Git
```bash
git add .
git commit -m "feat(storefront): add wallet notifications"
git add .
git commit -m "feat(storefront): add wallet notification navigation"
git add .
git commit -m "fix(wallet): make transaction notifications idempotent"
git push
```

## Next
0064–0066: email notifications and configurable notification preferences.
